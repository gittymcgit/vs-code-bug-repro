import classNames from 'classnames';
import { FC } from 'react';
import { FormattedMessage } from 'react-intl';

type UpsellProps = { bannerType: string };
const Upsell: FC<UpsellProps> = ({ bannerType }) => {
  return (
    <div>
      <div>
        <h1>test 21ww1!</h1>
      </div>
    </div>
  );
};
export default Upsell;
